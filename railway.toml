[build]
builder = "DOCKERFILE"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
startCommand = "java -jar app.jar"

[deploy.healthCheck]
enabled = true
path = "/health"
initialDelay = 15
periodSeconds = 10
timeoutSeconds = 5
failureThreshold = 3